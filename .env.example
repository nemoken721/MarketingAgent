# =================================
# Marty - 環境変数設定
# =================================
# 本番環境では Vercel Dashboard で設定してください
# 開発環境では .env.local にコピーして使用してください
# =================================

# ---------------------------------
# Node 環境
# ---------------------------------
NODE_ENV=development  # development | production | test

# ---------------------------------
# Google AI (Gemini)
# ---------------------------------
# https://aistudio.google.com/app/apikey から取得
GOOGLE_GENERATIVE_AI_API_KEY=your_gemini_api_key_here

# ---------------------------------
# Supabase
# ---------------------------------
# Project Settings > API から取得
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Supabase Auth Email Hook の Webhook Secret（オプション）
# Authentication > Hooks > Email Hook で設定した Secret
SUPABASE_WEBHOOK_SECRET=your_supabase_webhook_secret

# ---------------------------------
# Stripe（決済処理）
# ---------------------------------
# https://dashboard.stripe.com/apikeys から取得
# テスト用キーは sk_test_、本番用は sk_live_ で始まる
STRIPE_SECRET_KEY=your_stripe_secret_key
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=your_stripe_publishable_key

# Webhook Endpoint Secret
# https://dashboard.stripe.com/webhooks から取得
# Endpoint URL: https://your-domain.com/api/stripe/webhook
STRIPE_WEBHOOK_SECRET=your_stripe_webhook_secret

# ---------------------------------
# Sentry（エラー監視）
# ---------------------------------
# https://sentry.io/ でプロジェクトを作成して取得
# 開発環境では省略可（エラー監視が無効化されます）
SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id
NEXT_PUBLIC_SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id

# Sentry Organization & Project（ソースマップアップロード用）
SENTRY_ORG=your_sentry_org
SENTRY_PROJECT=your_sentry_project

# Sentry Auth Token（ビルド時のソースマップアップロード用）
# https://sentry.io/settings/account/api/auth-tokens/ から取得
# Scope: project:releases, project:write
SENTRY_AUTH_TOKEN=your_sentry_auth_token

# ---------------------------------
# OpenAI API（画像生成 - DALL-E 3）
# ---------------------------------
# https://platform.openai.com/api-keys から取得
OPENAI_API_KEY=your_openai_api_key

# ---------------------------------
# Resend（トランザクションメール）
# ---------------------------------
# https://resend.com/api-keys から取得
RESEND_API_KEY=your_resend_api_key

# 送信元メールアドレス（ドメイン認証済み）
# 例: noreply@yourdomain.com
RESEND_FROM_EMAIL=noreply@marty.example.com

# 管理者メールアドレス（お問い合わせ受信用）
ADMIN_EMAIL=admin@yourdomain.com

# ---------------------------------
# Instagram API（投稿連携）
# ---------------------------------
# Meta for Developers で作成したアプリから取得
# https://developers.facebook.com/apps/
INSTAGRAM_APP_ID=your_instagram_app_id
INSTAGRAM_APP_SECRET=your_instagram_app_secret

# ---------------------------------
# X (Twitter) API（投稿連携）
# ---------------------------------
# https://developer.twitter.com/ から取得
X_API_KEY=your_x_api_key
X_API_SECRET=your_x_api_secret
X_BEARER_TOKEN=your_x_bearer_token

# ---------------------------------
# Cron Jobs（Vercel Cron）
# ---------------------------------
# Cron Job の認証用シークレット
# ランダムな文字列を生成（例: openssl rand -base64 32）
# Vercel Dashboard の Cron Jobs 設定で同じ値を使用
CRON_SECRET=your_cron_secret

# ---------------------------------
# 暗号化キー（WordPress構築機能）
# ---------------------------------
# サーバー認証情報（SSH パスワード等）を暗号化するための鍵
# 必須: 32文字以上のランダムな文字列
# 生成方法: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=your_64_character_encryption_key_here

# ---------------------------------
# アプリケーション設定
# ---------------------------------
# 本番環境の URL（メールテンプレートで使用）
NEXT_PUBLIC_APP_URL=http://localhost:3000  # 本番: https://marty.example.com
